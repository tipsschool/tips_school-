school-website/
│── app.py
│── requirements.txt
│── Procfile
│── templates/
│   └── (HTML files)
│── static/
│   └── (CSS, images, JS)
│── uploads/   (can be empty)
git init
git add .
git commit -m "Add Flask school website code"
git branch -M main
git remote set-url origin https://github.com/YOUR-USERNAME/school-website.git
git push -u origin main
